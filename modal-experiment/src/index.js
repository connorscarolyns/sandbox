import { Carousel, initTE } from "tw-elements";
initTE({ Carousel });

// Initialization for ES Users
import {
    Modal,
    Ripple,
    initTE,
  } from "tw-elements";
  
  initTE({ Modal, Ripple });

